# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Prodrivetime_ios' do

  use_frameworks!
  
  # firebase messaging for push notification
  pod 'Firebase/Core'
  pod 'Firebase/Messaging'
  pod 'SwiftyBeaver'

end
